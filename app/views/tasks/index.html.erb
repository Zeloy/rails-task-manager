
<h1>Task List</h1>

<% @tasks.each do |task| %>
  <%= link_to task.name, task_path(task) %>
  <h5><%= task.name.upcase %></h5>
  <p><%= task.status %></p>
  <%= link_to 'Delete task', task_path(task), method: :delete %>
<% end %>
